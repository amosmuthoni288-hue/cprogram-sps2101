#include<studio.h>
#Define PI 3.1416
int main(){
	float radius,height,area,volume;
	printf("Enter radius of cylider")
	printf("Enter height of cylinder")
	area =PI * radius * radius + 2PI * radius * height;
	volume=M_PI * radius * radius * height;
	// output result
	return 0;
}